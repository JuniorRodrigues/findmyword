import React from 'react';
import ReactDOM from 'react-dom/client';
import FindMyWordApp from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <FindMyWordApp />
  </React.StrictMode>
);
